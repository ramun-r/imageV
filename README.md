# imageV

An OpenGL image viewer

## Clone the repository

```bash
git clone --recursive https://github.com/ramun-r/imageV.git
```

## Build the project

```powershell
cmake -S . -B build
cmake --build build
```

### Run the executable

```powershell
.\build\imageV.exe
```
